import { Controller } from '@nestjs/common';

@Controller('documents/:documentId/versions')
export class VersionsController {
  // TODO: Implement version endpoints
  // GET /versions - List all versions
  // POST /versions - Create new version (snapshot)
  // GET /versions/:version - Get specific version
  // POST /versions/:version/restore - Restore to version
}
